
var allCharacters = new Array();
var selectedCharacter = "";

function equipItem(item)
{
	var itemType = item.type;
	selectedCharacter.itemSlot = itemType;
	
}

function unequipItem(item)
{
	var itemType = item.type;
	
}

function getStats(c)
{
	var headObj = c.headSlot;
	headStat
	var chestObj = c.chestSlot;
	var handObj = c.handSlot ;
	
}

